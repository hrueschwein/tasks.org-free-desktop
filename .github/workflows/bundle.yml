name: Assemble bundle

on:
  push:
    branches:
      - main
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  desktop-jar:
    runs-on: ubuntu-latest
    needs: [ ]
    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK 21
        uses: actions/setup-java@v6
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Build desktop jar
        env:
          POSTHOG_KEY: ${{ secrets.POSTHOG_KEY }}
        run: ./gradlew :composeApp:desktopJar -Prelease

      - name: Upload desktop jars
        uses: actions/upload-artifact@v7
        with:
          name: desktop-jar
          path: |
            cert4android/build/libs/cert4android-jvm.jar
            composeApp/build/libs/composeApp-desktop*.jar
            data/build/libs/data-jvm.jar
            kmp/build/libs/kmp-jvm.jar

  desktop:
    runs-on: ubuntu-latest
    needs: [ desktop-jar ]
    strategy:
      matrix:
        include:
          - platform: linux-tar-gz-amd64
            target: linux-tarball
            flags: "-Kapp.machines=linux.amd64.glibc"
    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK 21
        uses: actions/setup-java@v6
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Download desktop jars
        uses: actions/download-artifact@v8
        with:
          name: desktop-jar

      - uses: ./.github/actions/install-conveyor

      - name: Cache Conveyor downloads
        uses: actions/cache@v6
        with:
          path: ~/.cache/hydraulic/conveyor
          key: conveyor-${{ matrix.platform }}

      - name: Generate signing key if absent
        env:
          CONVEYOR_AGREE_TO_LICENSE: 1
          SIGNING_KEY: ${{ secrets.CONVEYOR_SIGNING_KEY }}
        run: |
          if [ -z "$SIGNING_KEY" ]; then
            conveyor keys generate --no-passphrase
          fi

      - name: Build ${{ matrix.platform }} desktop
        working-directory: composeApp
        env:
          CONVEYOR_AGREE_TO_LICENSE: 1
          SIGNING_KEY: ${{ secrets.CONVEYOR_SIGNING_KEY }}
        run: |
          [ -z "$SIGNING_KEY" ] && unset SIGNING_KEY
          conveyor ${{ matrix.flags }} make ${{ matrix.target }}

      - name: Upload desktop-${{ matrix.platform }}
        uses: actions/upload-artifact@v7
        with:
          name: desktop-${{ matrix.platform }}
          path: composeApp/output/

  appimage:
    runs-on: ubuntu-latest
    needs: [ desktop ]
    strategy:
      matrix:
        include:
          - arch: x86_64
            conveyor-arch: amd64
    steps:
      - uses: actions/checkout@v7
        with:
          sparse-checkout: composeApp/AppRun

      - name: Download linux tarball
        uses: actions/download-artifact@v8
        with:
          name: desktop-linux-tar-gz-${{ matrix.conveyor-arch }}

      - name: Create AppDir from tarball
        run: |
          tarball=$(ls tasks-org-*-linux-${{ matrix.conveyor-arch }}.tar.gz)
          mkdir -p AppDir
          tar -xzf "$tarball" --strip-components=1 -C AppDir
          cp composeApp/AppRun AppDir/
          sed 's|Exec=.*|Exec=tasks-org|' AppDir/share/applications/org.tasks.desktop > AppDir/org.tasks.desktop
          cp AppDir/share/icons/hicolor/256x256/apps/tasksorg-llc-tasks-org.png AppDir/tasksorg-llc-tasks-org.png

      - name: Download appimagetool
        run: |
          curl -fsSL -o appimagetool https://github.com/AppImage/appimagetool/releases/download/1.9.1/appimagetool-x86_64.AppImage
          echo "ed4ce84f0d9caff66f50bcca6ff6f35aae54ce8135408b3fa33abfc3cb384eb0  appimagetool" | sha256sum -c
          chmod +x appimagetool

      - name: Build AppImage
        run: >
          ARCH=${{ matrix.arch }} ./appimagetool --appimage-extract-and-run
          -u "gh-releases-zsync|tasks|tasks|latest|tasks-org-linux-${{ matrix.arch }}.AppImage.zsync"
          AppDir tasks-org-linux-${{ matrix.arch }}.AppImage

      - name: Upload AppImage
        uses: actions/upload-artifact@v7
        with:
          name: desktop-linux-appimage-${{ matrix.arch }}
          path: |
            tasks-org-linux-${{ matrix.arch }}.AppImage
            tasks-org-linux-${{ matrix.arch }}.AppImage.zsync
